# Index

Compiled from events by the `compile` loop. Do not edit by hand: edits are overwritten on
the next compile, and a hand-written claim has no event id to cite.

Every claim on every page in this directory carries a citation of the form `[evt_...]`.
The `lint` loop fails on any claim that does not.

_No pages yet. Run `kourob ingest` and then `kourob loop run compile`._
