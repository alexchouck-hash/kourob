# Brief

The design documents, unchanged, as source material. `07-kourob-development.md` is the
current one and supersedes `06`.

| File | Subject | Present |
|---|---|---|
| `01-*.md` | the repo as memory | **missing** |
| `02-*.md` | distillation | **missing** |
| `03-*.md` | own data and tools | **missing** |
| `04-*.md` | event-sourced streams | **missing** |
| `05-*.md` | the harness | **missing** |
| `06-ourob-development.md` | superseded by 07 | **missing** |
| `07-kourob-development.md` | the current brief | yes |

Documents 01 to 06 were not supplied with 07. Everything in the M0 skeleton is derived
from 07 alone, which is self-contained for M0 and M1. Two places will want the missing
documents before their milestone starts:

- **`docs/brief/02` (distillation)** is cited by brief section 9 and section 15 for the
  gold-set and per-class evaluation discipline that M5 depends on.
- **`docs/brief/04` (event-sourced streams)** underpins the store and gate design in M1.

Drop them in this directory when they are available. Nothing needs to change to absorb
them; they are read, not imported.
